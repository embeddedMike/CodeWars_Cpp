#include <iostream>
#include <iterator>
#include <regex>
#include <string>
class ComposingSquaredStrings {
 public:
  static std::string compose(const std::string &s1, const std::string &s2);
  static std::vector<std::string> vectorOfPartialStrings(
      const std::string &strng);
};
